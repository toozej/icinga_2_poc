/**
 * The GraphiteWriter type writes check result metrics and
 * performance data to a graphite tcp socket.
 */
library "perfdata"
object GraphiteWriter "graphite" {
  host = "icinga-graphite"
  port = "2003"
  enable_send_thresholds = true
  enable_send_metadata = false
}
